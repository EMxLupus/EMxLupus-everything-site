export const founder_open   = (handle: string) => void 0;
export const social_click   = (handle: string, platform: "warpcast"|"x"|"website") => void 0;
export const filter_add     = (tag: string) => void 0;
export const filter_remove  = (tag: string) => void 0;
export const search_change  = (q: string) => void 0;
export const sort_change    = (mode: "relevance"|"az") => void 0;

